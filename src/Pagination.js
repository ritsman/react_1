import React from "react";
import { Pagination } from "semantic-ui-react";

const PaginationEx = () => (
  <Pagination defaultActivePage={5} totalPages={10} floated="right" />
);

export default PaginationEx;
